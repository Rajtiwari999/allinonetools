function runOCR(){
  alert('Client-side OCR requires Tesseract.js or server OCR. This UI is ready. For local demo, integrate Tesseract.js and process PDF pages as images.');
  document.getElementById('result').innerText = 'Placeholder: integrate OCR library (tesseract.js) for actual extraction.';
}
